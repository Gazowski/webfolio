import React from 'react'

function Title(title){
    console.log(title)
    return(
        <div>
        <h1>{title.title}</h1>
        <h1>le titre</h1>
        </div>
    )
}

export default Title;